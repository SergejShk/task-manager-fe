import styled from 'styled-components';

export const ErrorText = styled.span`
  position: absolute;
  left: 50%;
  bottom: -15px;
  transform: translateX(-50%);
  font-size: 10px;
  color: #e73f3f;
`;
